import os

# Create all view components

os.makedirs('src/views/projects', exist_ok=True)
os.makedirs('src/views/credentials', exist_ok=True)
os.makedirs('src/views/deploy', exist_ok=True)
os.makedirs('src/views/settings', exist_ok=True)

print("Directories created")
